export const metadata = {
  title: 'Test Invoice Creation',
  description: 'Create a test invoice for Nick Schaffer',
};

export default function TestInvoiceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 